apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-kargo-app.fullname" . }}
  labels:
    {{- include "demo-kargo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-kargo-app.selectorLabels" . | nindent 4 }}
